package com.example.e_disaster.data.mapper

import com.example.e_disaster.data.local.database.SyncStatus
import com.example.e_disaster.data.local.database.entities.DisasterAidEntity

object DisasterAidMapper {
    // Note: DisasterAid model is currently commented out
    // This mapper will be completed when the model is uncommented
    // For now, providing basic structure

    // Entity → Model (placeholder - will be implemented when model is available)
    // fun DisasterAidEntity.toModel(): DisasterAid { ... }

    // Model → Entity (placeholder)
    // fun DisasterAid.toEntity(...): DisasterAidEntity { ... }

    // DTO → Entity (placeholder)
    // fun DisasterAidDto.toEntity(...): DisasterAidEntity { ... }
}

